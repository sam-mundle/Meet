install node.js
make sure that npm commands are included in PATH

read about what npm is (node package manager)

The following two packages allow for automatic updating
of your site/server so you don't have to restart everytime

npm install nodemon -g
npm install webpack -g

make sure these two commands are included in PATH

npm install mysql
npm install express
npm install handlebars
npm install express-session
npm install express-mysql-session --save


Learn bootstrap to make really easy HTML formatting

-Swank out